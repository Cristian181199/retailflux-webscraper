# Core Dependencies
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.11.0

# Vector and AI Support
pgvector>=0.2.0
numpy>=1.24.0

# Configuration Management
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# API Framework (removed - not needed for scraper)
# fastapi and uvicorn moved to separate API service

# AI and NLP (lightweight only)
openai>=1.0.0
tiktoken>=0.4.0
# sentence-transformers removed to reduce Docker image size

# Text Processing (removed heavy packages)
# nltk and spacy removed to reduce Docker image size

# Utilities
python-dateutil>=2.8.0
requests>=2.31.0
httpx>=0.24.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
